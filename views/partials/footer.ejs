<div class="flex flex-col min-h-[30vh]">
    <footer class="footer text-xl text-green-600 font-mono bg-slate-800 text-light mt-auto text-right mr-1 py-4">
        <% 
            var monthNames = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
            var currentMonth = new Date().getMonth();
        %>
        Made by Subhajit @<%= (`${new Date().getFullYear()} ${monthNames[currentMonth]}`) %>
    </footer>
</div>
</body>

</html>
